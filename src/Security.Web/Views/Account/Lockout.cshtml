@{
    ViewData["Title"] = "Account Locked";
    Layout = "~/Views/Shared/_LoginLayout.cshtml";
}

<div class="login-container">
    <div class="glass-card login-card" style="text-align:center;">
        <div class="login-header">
            <div class="login-logo">🔒</div>
            <h1 class="login-title">Account locked</h1>
            <p class="login-subtitle">
                Too many failed login attempts. Your account has been locked for
                <strong>15 minutes</strong>. Please try again later.
            </p>
        </div>
        <a asp-controller="Account" asp-action="Login" class="glass-btn glass-btn-secondary mt-3">
            Back to login
        </a>
    </div>
</div>
